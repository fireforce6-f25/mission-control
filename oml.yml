# Project's properties
project:
   title: Mission Operations
   description: This is the mission-operations project
   group: io.github.fireforce6
   version: 0.1.0

# Maven repositories
repositories:
   - https://repo.maven.apache.org/maven2

# Maven dependencies
dependencies:
   - io.github.fireforce6:sierra-method:0.1.0

# Project's datasets
datasets:
   mission-operations: # configured in fuseki.ttl
      label: Mission Operations
      description: The set of ontologies in this mission-operations project
      iri: https://fireforce6.github.io/mission-operations/bundle
      sources: [ 'src/oml' ]
      queries: src/sparql